<Window x:Class="Pks_1kr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:Pks_1kr.ViewModels"
        Title="Управление библиотекой книг" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Панель фильтрации -->
        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Фильтры" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Поиск:" Margin="5" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="5" Padding="3"/>
                    
                    <TextBlock Grid.Column="2" Text="Автор:" Margin="5" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="3" ItemsSource="{Binding Authors}" 
                              SelectedItem="{Binding SelectedAuthor}"
                              DisplayMemberPath="FullName" Margin="5" Padding="3"/>
                    
                    <TextBlock Grid.Column="4" Text="Жанр:" Margin="5" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="5" ItemsSource="{Binding Genres}" 
                              SelectedItem="{Binding SelectedGenre}"
                              DisplayMemberPath="Name" Margin="5" Padding="3"/>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- DataGrid с книгами -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Books}" 
                  SelectedItem="{Binding SelectedBook}"
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Автор" Binding="{Binding Author.FullName}" Width="150"/>
                <DataGridTextColumn Header="Жанр" Binding="{Binding Genre.Name}" Width="100"/>
                <DataGridTextColumn Header="Год" Binding="{Binding PublishYear}" Width="60"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="120"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding QuantityInStock}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Добавить книгу" Width="120" Height="30" Margin="5" 
                    Command="{Binding AddBookCommand}"/>
            <Button Content="Изменить книгу" Width="120" Height="30" Margin="5" 
                    Command="{Binding EditBookCommand}"/>
            <Button Content="Удалить книгу" Width="120" Height="30" Margin="5" 
                    Command="{Binding DeleteBookCommand}"/>
            <Button Content="Авторы" Width="120" Height="30" Margin="5" 
                    Command="{Binding ManageAuthorsCommand}"/>
            <Button Content="Жанры" Width="120" Height="30" Margin="5" 
                    Command="{Binding ManageGenresCommand}"/>
            <Button Content="Обновить" Width="120" Height="30" Margin="5" 
                    Command="{Binding RefreshCommand}"/>
        </StackPanel>
    </Grid>
</Window>